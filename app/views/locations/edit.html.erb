<h1>Editing location</h1>

<%= render 'form' %>

<%= link_to 'Show', @location %> |
<%= link_to 'Back', locations_path %>

<% @units.each do |unit| %>
  <%= render 'units/form', {unit: unit, location: @location} %>
<% end %>
